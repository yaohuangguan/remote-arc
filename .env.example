# Local agent identity
REMOTE_LINK_DEVICE_ID=sam-pc
REMOTE_LINK_DEVICE_NAME=SamPC
REMOTE_LINK_RELAY_URL=wss://remote.samyao.me

# Never commit these. The relay and agent tokens are separate.
REMOTE_LINK_AGENT_TOKEN=
REMOTE_LINK_MCP_ACCESS_KEY=

# Local MCP permissions: safe | developer | full
REMOTE_LINK_MODE=safe
REMOTE_LINK_ALLOW_CORE_CALL=0
